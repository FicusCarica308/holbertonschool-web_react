// Teacher interface in a namespace named Subjects ( Namespace & Declaration merging )

namespace Subjects {
    export interface Teacher {
        firstName: string,
        lastName: string,
    }
}
